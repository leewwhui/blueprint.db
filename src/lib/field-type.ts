export enum FieldType {
  INT = "INT",
  VARCHAR = "VARCHAR",
  DATETIME = "DATETIME",
  BOOLEAN = "BOOLEAN",
}

export enum ColumnConstraints {
  PRIMARY_KEY = "PRIMARY_KEY",
  NOT_NULL = "NOT_NULL",
  UNIQUE = "UNIQUE",
}
